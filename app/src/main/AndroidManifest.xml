<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dupre.stephen.channelmessagingdupres">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_channel" />
        <activity android:name=".activity_channel_list" />
        <activity android:name=".activity_friends" />
        <activity android:name=".channels_list_row" />
        <activity android:name=".item_friend_layout" />
        <activity android:name=".row_message_layout"></activity>
    </application>

</manifest>